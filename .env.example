# ===========================================
# Mealie Configuration
# ===========================================

# General
TZ=Europe/Istanbul
BASE_URL=http://localhost:9925
MEALIE_PORT=9925
ALLOW_SIGNUP=true
MAX_WORKERS=1
WEB_CONCURRENCY=1

# PostgreSQL
POSTGRES_USER=mealie
POSTGRES_PASSWORD=mealie
POSTGRES_DB=mealie

# SMTP (optional - for email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM_NAME=Mealie
SMTP_FROM_EMAIL=
SMTP_AUTH_STRATEGY=TLS
SMTP_USER=
SMTP_PASSWORD=

# OpenAI (optional - for recipe parsing)
OPENAI_API_KEY=
